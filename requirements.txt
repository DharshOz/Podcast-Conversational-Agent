# --- LLM client (Groq, OpenAI-SDK compatible) ---
openai>=1.30.0

# --- Vector DB (Qdrant Cloud) ---
qdrant-client>=1.10.0

# --- Embeddings ---
sentence-transformers>=2.7.0
torch>=2.2.0

# --- Session/user store ---
pymongo>=4.7.0

# --- Transcript parsing (Colab notebook use) ---
pyyaml>=6.0
tiktoken>=0.7.0

# --- Env var loading ---
python-dotenv>=1.0.0

# --- API layer ---
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9

# --- Auth ---
passlib[bcrypt]>=1.7.4
python-jose[cryptography]>=3.3.0